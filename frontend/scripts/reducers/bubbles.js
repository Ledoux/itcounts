import assign from 'lodash.assign'

export const REQUEST_GET_PAGE = 'REQUEST_GET_PAGE'

export default function bubbles (state = intialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
